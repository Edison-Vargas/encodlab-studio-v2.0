import { AppLayout } from "@/components/layout/AppLayout";
import { URLDecoder } from "@/components/url/URLDecoder";

const URLDecoderPage = () => {
  return (
    <AppLayout>
      <URLDecoder />
    </AppLayout>
  );
};

export default URLDecoderPage;