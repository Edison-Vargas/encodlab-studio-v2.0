import { AppLayout } from "@/components/layout/AppLayout";
import { URLEncoder } from "@/components/url/URLEncoder";

const URLEncoderPage = () => {
  return (
    <AppLayout>
      <URLEncoder />
    </AppLayout>
  );
};

export default URLEncoderPage;